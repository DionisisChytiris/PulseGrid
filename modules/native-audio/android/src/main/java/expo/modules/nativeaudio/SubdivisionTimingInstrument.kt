package expo.modules.nativeaudio

/**
 * Retained for call-site compatibility; timing investigation logging removed.
 */
internal class SubdivisionTimingInstrument {
  fun reset() {
    // no-op
  }

  fun recordTick(sequence: Long, timestampMs: Long, bpm: Double, subdivisionMultiplier: Int) {
    // no-op
  }
}
